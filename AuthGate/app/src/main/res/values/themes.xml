<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AuthGate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AuthGate" parent="Base.Theme.AuthGate" />

    <style name="Theme.SplashScreen" parent="Base.Theme.AuthGate" />

    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#3D93F8</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_screen_icon</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.AuthGate</item>
    </style>

</resources>

<!--<style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">-->

<!--</style>-->